# LangRobo Pi5 brain — Python dependencies.
# Install:  pip3 install --break-system-packages -r requirements.txt
# The brain itself (graph, agents, tools, services) is the editable package
# below; its own dependencies (langgraph/langchain stack, qdrant-client,
# fastembed, fastapi, uvicorn) are declared in src/langrobo_core/pyproject.toml.
-e ./src/langrobo_core

# LangGraph Studio dev server (langgraph dev) — dev tool, not a core dependency.
langgraph-sdk==0.1.74
langgraph-cli[inmem]==0.1.89

# Camera frame handling on the ROS side.
opencv-python-headless

# colcon --symlink-install needs setup.py develop (removed in setuptools 80+)
setuptools<80
