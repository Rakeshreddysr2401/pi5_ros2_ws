# ── LangGraph 1.x runtime (validated: graph builds + compiles with a checkpointer) ──
# Upgraded from 0.2/0.3 → 1.x on 2026-06-28. Pins are the exact versions validated
# in the dev venv. The graph/ zone (pure LangGraph, no ROS2) builds clean on these.
langgraph==1.2.6
langgraph-sdk==0.4.2
# Transitive deps of langgraph 1.x — pinned so the Pi5 install is deterministic.
langgraph-checkpoint==4.1.1
langgraph-prebuilt==1.1.0

langchain-core==1.4.8
langchain-openai==1.3.3
langchain-anthropic==1.4.8
langchain-google-genai==4.2.6
langchain-ollama==1.1.0
langchain-mcp-adapters==0.3.0

python-dotenv
opencv-python-headless
typing-extensions

# colcon --symlink-install needs setup.py develop (removed in setuptools 80+)
setuptools<80

# ── Dev-only (Mac / LangGraph Studio — NOT needed on the Pi5 runtime) ──
# langchain-community was previously listed but is unused in src/ (only name-dropped
# in the chat prompt); removed to drop a dead dependency.
langgraph-cli[inmem]>=0.2 ; python_version >= "3.11"
